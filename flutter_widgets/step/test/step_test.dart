import 'package:flutter_test/flutter_test.dart';
import 'package:step/time_axis.dart';

void main() {
  testWidgets('this is TimeAxis', (WidgetTester tester) async {
    await tester.pumpWidget(TimeAxis(

    ));
  });
}
